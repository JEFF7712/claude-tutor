# Tutor v2 — Active Teaching Tools Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the tutor skill with 4 active teaching tools (code execution, exercises, visual aids, web research) while preserving the existing 10-mode pedagogy, and apply light marketplace polish.

**Architecture:** Add a "Active Teaching Tools" capability layer section to the existing SKILL.md, reorganize the file with clear section headers, and update plugin metadata + README for v2.0.0. No structural changes — same single SKILL.md file, same plugin layout.

**Tech Stack:** Markdown (SKILL.md prompt), JSON (plugin.json)

**Spec:** `docs/superpowers/specs/2026-03-24-tutor-v2-design.md`

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `skills/tutor/SKILL.md` | Modify | Reorganize sections, add Active Teaching Tools, add tool integration guidance, consolidate session closure |
| `.claude-plugin/plugin.json` | Modify | Bump to 2.0.0, update description |
| `README.md` | Modify | Add v2 features, update teaching modes table |

---

### Task 1: Reorganize SKILL.md section headers

Restructure the existing content under clear named sections without changing any content. This establishes the skeleton that Task 2 will add to.

**Files:**
- Modify: `skills/tutor/SKILL.md`

**Current structure → New structure:**
```
CURRENT                          NEW
-------                          ---
# Adaptive Tutor                 # Adaptive Tutor (unchanged)
## Starting a Session            ## Opening Protocol (rename)
## Teaching Modes                ## Teaching Modes (unchanged)
## Mode Blending                 (move to after new tools section)
  ### Confusion Escalation       (moves with Mode Blending)
## Rules                         (move to after Mode Switching)
## Explicit Mode Commands        (fold into Mode Switching)
                                 ## Active Teaching Tools (Task 2)
                                 ## Mode Switching (renamed Mode Blending + Explicit Mode Commands)
                                 ## Session Rules (renamed Rules)
                                 ## Session Closure (new, consolidates existing)
```

**Note:** All line numbers below refer to the original file before any modifications. Use section headings and content to locate the right text after earlier steps shift things around.

- [ ] **Step 1: Rename "Starting a Session" to "Opening Protocol"**

Change `## Starting a Session` (originally line 10) to `## Opening Protocol`. Content stays identical.

- [ ] **Step 2: Move Mode Blending + Confusion Escalation after Teaching Modes placeholder**

Cut the `## Mode Blending` section including `### Confusion Escalation` (originally lines 84-101). These will be placed after the Active Teaching Tools section (which Task 2 adds). For now, move them to the end of the file, right before Rules.

Rename `## Mode Blending` to `## Mode Switching`.

- [ ] **Step 3: Fold Explicit Mode Commands into Mode Switching**

Move the `## Explicit Mode Commands` section (originally lines 119-129) into the Mode Switching section as a subsection. Change `## Explicit Mode Commands` to `### Explicit Mode Commands`.

- [ ] **Step 4: Rename "Rules" to "Session Rules"**

Change `## Rules` to `## Session Rules`. Content stays identical for now — Step 5 will remove one bullet to avoid duplication with Session Closure.

- [ ] **Step 5: Add Session Closure section**

Add a new `## Session Closure` section at the end of the file. Consolidate the existing session-ending behavior that's currently split across two places:

```markdown
## Session Closure

When a session ends (learner says "that's enough", "let's wrap up", or reaches a natural conclusion):

1. Summarize what was covered and what the learner demonstrated understanding of
2. Be direct about gaps — what needs more work
3. Suggest concrete next steps: what to study, what to practice, when to revisit
4. If the topic has remaining subtopics, list what's left to cover
```

This consolidates the "Session ending" row from the Mode Switching table and the "End sessions with" item from the ALWAYS rules. Remove the "End sessions with" bullet from Session Rules to avoid duplication (the Mode Switching table row stays as a trigger pointer to this section).

- [ ] **Step 6: Verify the final section order**

Confirm SKILL.md now reads in this order:
1. Frontmatter + intro paragraph
2. `## Opening Protocol`
3. `## Teaching Modes` (all 10 modes)
4. `## Mode Switching` (with Confusion Escalation + Explicit Mode Commands as subsections)
5. `## Session Rules` (NEVER/ALWAYS, minus the "End sessions" bullet)
6. `## Session Closure`

Note: Active Teaching Tools (Task 2) will be inserted between Teaching Modes and Mode Switching.

- [ ] **Step 7: Commit**

```bash
git add skills/tutor/SKILL.md
git commit -m "refactor(skill): reorganize SKILL.md section structure for v2"
```

---

### Task 2: Add Active Teaching Tools section

Add the 4 new capability definitions to SKILL.md, inserted between "Teaching Modes" and "Mode Switching".

**Files:**
- Modify: `skills/tutor/SKILL.md`

- [ ] **Step 1: Insert the Active Teaching Tools section**

Add the following after the last teaching mode (Mode 10: Progressive Recall Mentor) and before `## Mode Switching`:

```markdown
## Active Teaching Tools

You have tools beyond conversation. Use them when they genuinely help — never force them. If a tool is unavailable, fall back to conversational teaching.

### Live Code Execution

**Trigger:** Topic involves programming, math, data, or any concept you can demonstrate with runnable code.

**Behavior:**
- Write small examples (under 30 lines) and run them so the learner sees real output
- Use "predict then verify" — ask what the code will output before running it
- Support whatever language the learner is working in
- Keep each example focused on one concept

### Interactive Exercises

**Trigger:** The learner is practicing or needs hands-on reinforcement.

**Behavior:**
- Create a temporary exercise file (`/tmp/tutor-exercise.<ext>`) with a skeleton and instructions in comments
- Tell the learner to open it and fill in the implementation
- When they're done, read their code and run it — give feedback on correctness, style, and edge cases
- For test-driven exercises: write the tests first, have the learner make them pass

### Visual Aids

**Trigger:** Concept is abstract, involves relationships or flows, or the learner is in Visual Thinking Translator mode.

**Behavior:**
- Generate Mermaid diagrams as fenced code blocks for complex visuals
- Use ASCII diagrams and tables for simpler visuals
- Combine: Mermaid for the big picture, ASCII for zoomed-in details

Reach for these diagram types:
- `flowchart` — processes, decision trees, control flow
- `sequenceDiagram` — interactions, protocols, request/response
- `classDiagram` — relationships, hierarchies, data models
- `stateDiagram` — state machines, lifecycle
- `mindmap` — topic decomposition

### Web Research

**Trigger:** Topic needs current information, your training data may be outdated, or the learner asks "what's the latest on X?"

**Behavior:**
- Search the web to pull in current docs, examples, or explanations
- Cite sources when presenting researched information
- Research feeds into your teaching — you still teach, don't just paste results
```

- [ ] **Step 2: Verify placement**

Read the file and confirm Active Teaching Tools sits between Teaching Modes and Mode Switching.

- [ ] **Step 3: Commit**

```bash
git add skills/tutor/SKILL.md
git commit -m "feat(skill): add active teaching tools section (code, exercises, visuals, research)"
```

---

### Task 3: Add tool integration guidance to Mode Switching

Update the Mode Switching section to reference which tools each mode should reach for.

**Files:**
- Modify: `skills/tutor/SKILL.md`

- [ ] **Step 1: Add tool guidance after the Mode Switching intro**

Insert the following after the opening paragraph of Mode Switching ("Don't stick to one mode rigidly. Blend based on these signals:") and before the signals table:

```markdown
### Tool Integration

Tools are available in modes where they genuinely help. Not every mode needs tools — 5 of 10 are purely conversational.

| Mode | Available Tools |
|---|---|
| Mixed Practice Architect | Exercises, Code Execution |
| Mental Model Forge | Visual Aids |
| Visual Thinking Translator | Visual Aids |
| Active Recall Generator | Exercises, Code Execution |
| Analogy Bridge Tutor | Visual Aids (optional) |
| Simplified Learning Strategist | Visual Aids, Web Research |
| Progressive Recall Mentor | Code Execution |

Tools serve the pedagogy, not the other way around. If a diagram doesn't clarify, skip it. If code execution interrupts the teaching flow, don't use it.
```

- [ ] **Step 2: Update the "Session ending" row in the signals table**

Change the response for "Session ending" from `Progressive Recall summary with review questions.` to `Transition to Session Closure.` to point to the new consolidated section.

- [ ] **Step 3: Commit**

```bash
git add skills/tutor/SKILL.md
git commit -m "feat(skill): add tool integration guidance to mode switching"
```

---

### Task 4: Update plugin.json to v2.0.0

**Files:**
- Modify: `.claude-plugin/plugin.json`

- [ ] **Step 1: Bump version and update description**

Update `plugin.json`:

```json
{
  "name": "tutor",
  "description": "Adaptive tutor skill with 10 teaching modes and active teaching tools — runs code, creates exercises, generates diagrams, and researches topics. Makes Claude act as an interactive coach for learning any subject.",
  "version": "2.0.0",
  "author": {
    "name": "Rupan Sunderapandyan"
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add .claude-plugin/plugin.json
git commit -m "chore(plugin): bump version to 2.0.0"
```

---

### Task 5: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add v2 section and update teaching modes table**

Replace the full README content with:

```markdown
# Claude Tutor Plugin

An adaptive tutor skill for Claude Code that makes Claude act as an interactive coach for learning any topic.

## What's New in v2

- **Live Code Execution** — tutor writes and runs examples so you see real output
- **Interactive Exercises** — practice with real code challenges, get feedback on your solutions
- **Visual Aids** — Mermaid diagrams and ASCII visuals for abstract concepts
- **Web Research** — pulls in current docs and examples when topics need up-to-date info

## Install

### As a plugin

```
/plugin marketplace add JEFF7712/claude-tutor
/plugin install tutor@claude-tutor
```

### Manual

```bash
git clone https://github.com/JEFF7712/claude-tutor.git ~/.claude/skills/claude-tutor
```

## Usage

- `/tutor <topic>` — start a tutoring session
- Or just ask naturally: "teach me about quantum physics", "help me learn SQL", "explain how DNS works"

## Teaching Modes

The tutor auto-selects and blends 10 teaching modes based on your responses:

| Mode | What it does | Tools |
|------|-------------|-------|
| Socratic Drillmaster | Asks questions that lead you to the answer | — |
| Mixed Practice Architect | Interleaved drills mixing related concepts | Exercises, Code |
| Why-How Interrogator | Challenges surface-level understanding | — |
| Mental Model Forge | Builds frameworks: principles -> rules -> examples | Visuals |
| Visual Thinking Translator | Diagrams, tables, and flowcharts | Visuals |
| Active Recall Generator | Makes you summarize, create analogies, and build flashcards | Exercises, Code |
| Meta-Learning Coach | Checks what's working and adjusts the approach | — |
| Analogy Bridge Tutor | Explains concepts through familiar domains | Visuals |
| Simplified Learning | Breaks complex ideas down for beginners | Visuals, Research |
| Progressive Recall Mentor | Climbs from recall to application to synthesis | Code |

You can also switch modes explicitly: "quiz me", "explain it simpler", "use an analogy", "give me drills", "let's wrap up".

## Credits

Teaching modes inspired by [@AI_with_jasmin](https://x.com/AI_with_jasmin).
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs(readme): update for v2 with active teaching tools"
```

---

### Task 6: Final verification

- [ ] **Step 1: Read the final SKILL.md end-to-end**

Read `skills/tutor/SKILL.md` and verify:
- Section order: Opening Protocol → Teaching Modes → Active Teaching Tools → Mode Switching → Session Rules → Session Closure
- No duplicate content between Session Rules and Session Closure
- Tool Integration table is in Mode Switching
- Explicit Mode Commands is a subsection of Mode Switching
- Confusion Escalation is a subsection of Mode Switching
- All 10 modes are intact and unmodified

- [ ] **Step 2: Read plugin.json and README**

Verify version is 2.0.0 and README has the new Tools column in the teaching modes table.

- [ ] **Step 3: Verify git log**

Run `git log --oneline -6` and confirm all commits are clean.
